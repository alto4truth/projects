title: From Image to Music Language: A Two-Stage Structure Decoding Approach for Complex Polyphonic OMR
source: arxiv
year: 2026
document_id: from-image-to-music-language-a-two-stage-structure-decoding-approach-for-complex-polyphonic-omr
authors: Nan Xu; Shiheng Li; Shengchao Hou
page: 1
section: paraff
chunk: 16
source_ref: https://arxiv.org/pdf/2604.20522v1

x = (picker, ptFactor)
Output: ClusterEvaluation
v.count←v.count+ 1
ni←arg max i

pi /(visits(c i) + 1)

ifni=∅:v.count← ∞;returnEVALUATE(. . . )
32

v.pt←v.pt+ min(100,−lnp ni)
ifv.pt>100·λ:v.count← ∞;returnEVALUATE(. . . ) // prune
switchv.type:
casePass:
ife ni =EOS:
eval←EVALUATE(cluster, tip, pt)
ifno residueorfatal:v.count← ∞;returneval
// start new voice: re-enter BOS, re-query model
createc ni as Pass node;p←PREDICTCLUSTER(tip+ 1)
else:
eni.order←tip
createc ni as Division node;p←e ni.divisionVector
caseDivision:e cur .div←ni; createc ni as Dots node;p←e cur .dotsVector
caseDots:
ecur .dots←ni;eval←EVALUATE(cluster, tip, pt)
ifno residueorfatal:v.count← ∞;returneval// leaf
createc ni as Pass node;p←PREDICTCLUSTER(tip)
result←DEDUCE(c ni,ctx)
ifselfEval̸=∅andresult.fatal: rollback orders>tip;returnselfEval
returnresult
B.4 Cluster Evaluation
EVALUATEis called at every leaf or pruning point. It receives the partial order assignment, computes
ticks cumulatively, and returns a scalar loss.
Tick propagation.Events are iterated in order. The current voice-local tick advances by the
estimated element duration after each event; when a new voice starts the tick resets to zero. If
the model’s predicted tickˆti is available as an integer, it is used as a lower bound for ti (allowing
parallelism to be encoded via predisposition). Time-warped events (e.g. triplets) have their duration
scaled by2/3.
Tick twist τ.A well-engraved measure should be spatially monotone: later notes appear further
right. Thetwistmetric quantifies violations in the (x, t) plane. For each consecutive pair in x-sorted
order, the angleθbetween the(∆x/x span,∆t/t span)vector and the diagonal is measured:
τ= max
pairs

4
π arctan ∆t/tspan
∆x/xspan
−1
2
.
When all adjacent events progress monotonically the angle equals 45, giving τ= 0 . A reversal (later
tick to the left of earlier tick) drivesτ→1;τ≥1is a fatal error.
Combined loss.Let T= 1920 (whole-note duration in ticks), r = residue count, v = voice count, s
= mean unused voice time,π= pretentiousness:
L= RMSE(t, ˆt)
T +τ+ 0.2r+ 0.002v+ 0.4 s
T + 0.02π.
The numeric weights (e.g., 0.2,0.002,0.4,0.02 ) were chosen by manual, empirical tuning to balance
these error terms in practice. Each term captures a distinct failure mode: tick deviation from the
model’s estimate; spatial monotonicity violation; unassigned events; overly split voices; temporal
gaps within a voice; and implausibility of the chosen path.
B.5 Cluster-Level Search
SOLVECLUSTERdrives the search loop. It initialises a root PASSnode, then repeatedly descends
from the root until the quota is exhausted, the tree is fully explored, or a sufficiently low loss is
reached.
33
